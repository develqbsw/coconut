<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<artifactId>droid-core-api</artifactId>
	<packaging>jar</packaging>
	<organization>
		<name>QBSW a.s.</name>
		<url>http://www.qbsw.sk</url>
	</organization>
	<name>Android core api</name>
	<parent>
		<groupId>sk.qbsw</groupId>
		<artifactId>droid-core</artifactId>
		<version>1.11.6-SNAPSHOT</version>
	</parent>
	<dependencies>

		<!-- dependency to android -->
		<dependency>
			<groupId>com.google.android</groupId>
			<artifactId>android</artifactId>
			<version>${android-platform.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Gson: Java to Json conversion -->
		<dependency>
			<groupId>com.google.code.gson</groupId>
			<artifactId>gson</artifactId>
			<version>${gson.version}</version>
			<scope>compile</scope>
		</dependency>
		<!-- Http client to comunicate to server api -->
		<dependency>
			<groupId>sk.qbsw</groupId>
			<artifactId>core-api</artifactId>
			<version>${core.version}</version>
			<scope>compile</scope>
		</dependency>
		<!-- END Http client to comunicate to server api -->

		<!-- HTTP -->
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpmime</artifactId>
			<version>${httpcomponents.version}</version>
			<scope>compile</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
			<version>${httpcomponents.version}</version>
			<scope>compile</scope>
		</dependency>

		<!-- dependencies for QBSW modules -->

		<dependency>
			<groupId>sk.qbsw</groupId>
			<artifactId>droid-core-logging</artifactId>
			<version>${droid.version}</version>
			<scope>compile</scope>
		</dependency>

		<dependency>
			<groupId>sk.qbsw</groupId>
			<artifactId>droid-core-utils</artifactId>
			<version>${droid.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<inherited>true</inherited>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${compiler.version}</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
					<optimize>true</optimize>
					<debug>true</debug>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>